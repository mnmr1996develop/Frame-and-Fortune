spring:
  application:
    name: follow-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/frame_and_fortune
    username: mnmr1996development
    password: mnmr1996development
  flyway:
    enabled: false


server:
  port: 8081


eureka:
  client:
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

services:
  user:
    baseurl: http://USER-SERVICE/api/v1/users